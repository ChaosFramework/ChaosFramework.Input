<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">

  <!-- Project Metadata -->
  <PropertyGroup>
    <AssemblyName>ChaosFramework.Input</AssemblyName>
    <RootNamespace>ChaosFramework.Input</RootNamespace>
    <ProjectGuid>{3B397619-8336-43A0-B615-12905389D6DA}</ProjectGuid>
  </PropertyGroup>

  <!-- Properties -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.Targets/Properties/Common.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.Targets/Properties/OutputType/Library.targets" />

  <!-- Configurations -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.Targets/Configurations.targets" />

  <!-- Included Files -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.Targets/StandardResources.targets" />
  <ItemGroup>
    <Compile Include="source\**\*.cs" />

    <None Include="Resources\**\*.txt" />
    <None Include="Resources\**\*.xml" />
  </ItemGroup>

  <!-- References -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/ProjectReferences/ChaosUtil.Debug.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/ProjectReferences/ChaosUtil.Reflection.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/ProjectReferences/ChaosUtil.Serialization.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/ProjectReferences/ChaosFramework.Collections.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/ProjectReferences/ChaosFramework.Core.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/ProjectReferences/ChaosAnalyzers.ClassIntegrity.Types.targets" />

  <!-- Analyzers -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/Analyzers/ChaosAnalyzers.CleanCode.targets" />
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/Analyzers/ChaosAnalyzers.ClassIntegrity.targets" />

  <!-- Cleanup Task -->
  <Import Project="$(SolutionDir)submodules/ChaosPresets.References/BuildTasks/ChaosBuild.CodeFormatter.Tasks.ChangedProjectFiles.targets" />

</Project>
